import React from 'react'

function AddToFavorites({children}) {
    return (
    
        <>
        <button>{children}</button>
        </>
            
        
    )
}

export default AddToFavorites
